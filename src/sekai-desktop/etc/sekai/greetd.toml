# SekaiOS — greetd 설정
#   /etc/greetd/config.toml 은 greetd 패키지 것이라 두고, 서비스가 이 파일을 쓰게 한다
#   (/usr/lib/systemd/system/greetd.service.d/sekai.conf).
[terminal]
# 로그인 화면은 7번 가상 터미널. 1번은 텍스트 콘솔로 남겨 둔다 (비상시 Ctrl+Alt+F1).
vt = 7

[default_session]
command = "/usr/bin/sekai-greeter-session"
user = "_greetd"
